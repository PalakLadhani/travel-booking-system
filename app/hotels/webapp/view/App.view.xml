<mvc:View
    controllerName="travel.hotels.hotels.controller.App"
    xmlns:mvc="sap.ui.core.mvc"
    xmlns="sap.m"
    displayBlock="true">

    <App id="app" busy="{appView>/busy}" busyIndicatorDelay="0">
        <!-- Router places child views (HotelsList, HotelDetail, Chat, Bookings) here -->
    </App>

</mvc:View>